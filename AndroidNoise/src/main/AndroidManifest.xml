<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.SecretSquirrel.AndroidNoise"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".model.NoiseRemoteApplication"
        android:theme="@style/AppTheme" >
        <service
            android:name=".services.NoiseRemoteService"
            android:label="AndroidNoise:NoiseRemoteService" />
        <service
            android:name=".services.NoiseDataService"
            android:label="AndroidNoise:NoiseDataService" />
        <service
            android:name=".services.ServiceLocator"
            android:label="AndroidNoise:ServiceLocator" />

        <activity
            android:name=".activities.ServerConnectActivity"
            android:label="@string/title_activity_server_connect" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ArtistListActivity"
            android:label="@string/title_activity_artist_list" >
        </activity>
    </application>

</manifest>
