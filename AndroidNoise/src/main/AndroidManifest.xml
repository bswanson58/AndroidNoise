<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.SecretSquirrel.AndroidNoise"
    android:versionCode="1"
    android:versionName="@string/software_version" >

    <!--uses-sdk (specified in gradle build script)
        android:minSdkVersion="9"
        android:targetSdkVersion="19" /-->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:name="com.SecretSquirrel.AndroidNoise.application.NoiseRemoteApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
            android:name="com.SecretSquirrel.AndroidNoise.services.NoiseRemoteService"
            android:label="AndroidNoise:NoiseRemoteService" />
        <service
            android:name="com.SecretSquirrel.AndroidNoise.services.NoiseDataService"
            android:label="AndroidNoise:NoiseDataService" />
        <service
            android:name="com.SecretSquirrel.AndroidNoise.services.EventHostService"
            android:label="AndroidNoise:EventHostService"/>

        <activity
            android:name="com.SecretSquirrel.AndroidNoise.activities.ServerActivity"
            android:label="@string/title_activity_shell"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.SecretSquirrel.AndroidNoise.activities.LibraryActivity"
                  android:label="@string/title_activity_shell"
                  android:launchMode="singleTop"/>

        <activity android:name=".activities.SettingsActivity"
                  android:label="Noise Settings"/>

        <meta-data android:name="com.crashlytics.ApiKey" android:value="35b3d3242e7b7cf6b9f547f09be1666642aa743d"/>
    </application>

</manifest>
